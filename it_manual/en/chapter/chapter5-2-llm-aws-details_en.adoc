
===== Supported Models

*OpenAI OSS*

Both available models are supported:

* gpt-oss-120b
* gpt-oss-20b

*Qwen*

All available Qwen models are supported:

* Qwen3 235B A22B 2507
* Qwen3 32B (dense)
* Qwen3-Coder-30B-A3B-Instruct

===== Supported Inference-profiles

*Anthropic*

* Claude Sonnet 4
* Claude Sonnet 4.5
* Claude Sonnet 4.6
* Claude Haiku 4.5
* Claude Opus 4.6
* Claude Opus 4.6

===== Supported Embedding Models

*Amazon Titan*

* Amazon Titan Text Embeddings V1
* Amazon Titan Text Embeddings V2

[.highlight]#*Note*: These models is fully supported by {application}. Alternative or extended models are also possible but are not included in the standard scope and are offered separately.#

===== Step-by-Step: Setting up AWS Bedrock

*Step 1: Activate AWS Bedrock*

* Open AWS Console: https://console.aws.amazon.com
* Navigate to Bedrock: enter “Bedrock” in the search bar
* Select region: e.g., eu-central-1 (Frankfurt) or us-east-1
* Enable Model Access:
** Click on *Model access* on the left
** *Manage model access*
** Select required models (e.g., OpenAI OSS, Meta, Anthropic, etc.)
** Click *Save changes*

*Notes:*

* Some 3rd‑party providers (e.g., Anthropic) require 5–15 minutes for activation
* Model Access is mandatory. IAM alone is not sufficient – models must be activated manually.
* Model IDs must match exactly. Incorrect or outdated IDs will cause AccessDeniedException.
* Region must be consistent. Model region, Bedrock API region, and IAM policy must match.

*Step 2: Configure IAM Permissions*

To allow an EC2 instance (or other AWS services) to access Bedrock, either an IAM Role *or* IAM User is required.

* IAM Role (recommended for AWS resources)
* IAM User with Access Keys (recommended for local developer environments)

|===
|Method | Advantages | Disadvantages

|IAM Role
| Automatic, secure temporary credentials via IMDS (Instance Metadata Service), no key management required 
| Only suitable for AWS resources (not for local developers)

|IAM User
| Usable for local tools & developers, even outside AWS 
| Permanent keys must be stored securely & rotated
|===

*Step 3: Create IAM Role or IAM User*

For accessing AWS Bedrock, either an IAM role or an IAM user must be created.
Custom IAM policies must be created before creating the IAM role or IAM user so they can be selected and assigned during creation.
The minimal policy for Bedrock access is:
```json
{
 "Version": "2012-10-17",
 "Statement": [
   {
     "Effect": "Allow",
     "Action": [
       "bedrock:InvokeModel",
       "bedrock:InvokeModelWithResponseStream"
     ],
     "Resource": "arn:aws:bedrock:*::foundation-model/*"
   }
 ]
}
```
→ Optional: Restrict access to a specific region, e.g., Frankfurt only:
```
"Resource": "arn:aws:bedrock:eu-central-1::foundation-model/*"
```

*Amazon ARN* (Amazon Resource Names) are unique identifiers used to reference AWS resources such as AI models. +
See: https://docs.aws.amazon.com/IAM/latest/UserGuide/reference-arns.html

Example ARN of a foundation model:
```
arn:aws:bedrock:eu-central-1::foundation-model/openai.gpt-oss-20b-1:0
```

*Components:*

* arn: – Resource format identifier
* aws: – AWS namespace
* bedrock: – Service
* eu-central-1: – Region
* foundation-model: – Resource type
* openai.gpt-oss-20b-1:0 – Model ID

Newer models use *Inference Profiles* to enable cross‑region load distribution. +
See https://docs.aws.amazon.com/de_de/bedrock/latest/userguide/inference-profiles-support.html

An inference profile invocation always contains the source region (`eu-central-1`), the profile call (`inference-profile`), the target regions (`eu`), and the model (`anthropic.claude-sonnet-4-6`).

Example ARN of an inference profile:
```
arn:aws:bedrock:eu-central-1::inference-profile/eu.anthropic.claude-sonnet-4-6
```
*Key components:*

* Source region: eu-central-1
* Account ID: <account-id></account-id>
* Resource type: inference-profile
* Profile/model: eu.anthropic.claude-sonnet-4-6 +
(distributed across multiple EU regions: eu-central-1, eu-north-1, eu-south-1, eu-south-2, eu-west-1, eu-west-3)

Policy for EU‑wide usage of foundation models + inference profiles:
```
{
        "Version": "2012-10-17",
        "Statement": [
                {
                        "Effect": "Allow",
                        "Action": [
                                "bedrock:InvokeModel",
                                "bedrock:InvokeModelWithResponseStream"
                        ],
                        "Resource": [
                                "arn:aws:bedrock:eu-*::foundation-model/*",
                                "arn:aws:bedrock:eu-*:<account-id>:inference-profile/*"
                        ]
                }
        ]
}
```
*Important:*

* eu-* for foundation-model, because inference profiles distribute across all EU regions
* eu-*:<account-id> for inference profiles, because they are account‑bound


*Step 4: Generate Access Keys (not required for IAM Role)*

1. AWS IAM Console → Users → Your user → Security credentials
2. Create access key
3. *Save the following for your application configuration:*
* AWS Access Key ID
* AWS Secret Access Key
* AWS Region 
* Model ID (e.g., openai.gpt-oss-20b-1:0)

Example:
```
AWS Access Key ID: AKIAIOSFODNN7EXAMPLE
AWS Secret Access Key: wJalrXUtnFEMI/K7MDENG...
AWS Region: eu-central-1
Model ID: openai.gpt-oss-20b-1:0
```

*Assigning IAM User / IAM Role to EC2 Instance*

*IAM Role (recommended)*: +
Assign the role to the EC2 instance via → EC2 → Instance → Actions → Security → Modify IAM role.

*IAM User*: +
For an IAM User the AWS credentials at the EC2 instance be stored in the application's configuration.


